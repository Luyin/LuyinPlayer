import LuyinPlayer from './index';

describe('Luyin Player', () => {
    describe('#Play', () => {
        it('Initialize', () => {
           const options = {

           };
           const luyinPlayer = new LuyinPlayer('luyin-dom', options);
        });
    });
});