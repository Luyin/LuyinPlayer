class LuyinPlayer {
    private selector: string;
    private options: object;

    constructor(selector: string, options: object) {
        this.selector = selector;
        this.options = options;
    }
}

export default LuyinPlayer;
